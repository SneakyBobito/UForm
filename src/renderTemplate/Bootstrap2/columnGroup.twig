<div class="row-fluid">
    {% for column in current.children -%}
    <div class="span{{ column.getAdaptiveWidth(render.colNumber) | round }}">
            {{- renderElement(column) -}}
    </div>
    {%- endfor %}
</div>
