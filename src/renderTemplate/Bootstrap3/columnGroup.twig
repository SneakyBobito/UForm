<div class="row">
    {% for column in current.children -%}
    <div class="col-md-{{ column.getAdaptiveWidth(render.colNumber) }}">
            {{- renderElement(column) -}}
    </div>
    {%- endfor %}
</div>
